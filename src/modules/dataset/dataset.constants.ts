const DATASET_DOMAINS = {
    CLIMATE: "CLIMATE",
    ENERGY: "ENERGY",
    POWER: "POWER"
} as const;

const DATASET_VISUALIZATION_TYPES = {
    INDIA_MAP: "INDIA_MAP",
    STATE_HEATMAP: "STATE_HEATMAP",
    LINE: "LINE",
    BAR: "BAR",
    AREA: "AREA"
} as const; 

const DATASET_STATUS = {
    PENDING: "PENDING",
    APPROVED: "APPROVED",
    REJECTED: "REJECTED"
} as const;

const DATASET_COLUMN_TYPES = {
    STRING: "STRING",
    NUMBER: "NUMBER",
    DATE: "DATE"
} as const;

export { DATASET_DOMAINS, DATASET_VISUALIZATION_TYPES, DATASET_STATUS, DATASET_COLUMN_TYPES };
